project_name: iris-classification-template
experiment_name: iris_mlp_dropout_robust
random_seed: 42

paths:
  raw_csv: data/raw/iris.csv
  dataset_metadata: data/raw/dataset_metadata.json
  processed_dir: data/processed/iris_mlp_dropout_robust
  preprocessor_artifact: models/iris_mlp_dropout_robust/preprocessor.json
  model_checkpoint: models/iris_mlp_dropout_robust/model.pt
  training_history_csv: reports/iris_mlp_dropout_robust/training_history.csv
  training_summary_json: reports/iris_mlp_dropout_robust/training_summary.json
  evaluation_json: reports/iris_mlp_dropout_robust/evaluation.json
  predictions_csv: reports/iris_mlp_dropout_robust/test_predictions.csv
  confusion_matrix_png: reports/iris_mlp_dropout_robust/confusion_matrix.png
  pairplot_png: reports/global/iris_pairplot.png
  feature_hist_png: reports/global/feature_histograms.png
  training_curves_png: reports/iris_mlp_dropout_robust/training_curves.png

data:
  target_column: species
  feature_columns:
    - sepal_length_cm
    - sepal_width_cm
    - petal_length_cm
    - petal_width_cm
  class_names:
    - setosa
    - versicolor
    - virginica
  test_size: 0.2
  validation_size: 0.2

preprocessing:
  strategy: robust

model:
  name: mlp_dropout_classifier

training:
  hidden_dims: [32, 16]
  learning_rate: 0.01
  epochs: 180
  batch_size: 16
  weight_decay: 0.0001

visualization:
  figure_dpi: 160
